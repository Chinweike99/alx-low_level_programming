#include "main.h"
#include <stdio.h>
/**
 * is_prime_number - A functionn that returns prime numbers
 * @n: The integer to be returned
 * Return: Always success
 */
int is_prime_number(int n)
{
	if (n % 2 != 0)
	{
		return (1);
	}
	return (0);
}
