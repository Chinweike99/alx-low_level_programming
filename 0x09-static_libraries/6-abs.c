#include "main.h"
/**
 * _abs - Coputes absolute value of the integer
 * @n: The int to check
 * Return: The absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
